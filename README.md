# BookReader

```markdown
一款轻量 epub reader web 应用

1. 亮点功能
文字阅读
在线转音频，多种音色选择
离线音频下载，支持断点续传
```


## Quickstart

```markdown
1. backend
cd epub-tts-backend
source venv/bin/activate
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

2. frontend
npm install

npm run dev
```